---
name: Bug report
about: Something in omniline is broken
title: ""
labels: bug
---

**Describe the bug**
A clear description of what's wrong.

**Harness**
Which one — Claude Code, antigravity-cli, Codex, or the installer itself
(not tied to a harness)?

**Steps to reproduce**
1. ...
2. ...

**Expected behavior**
What you expected to happen instead.

**Actual behavior**
What actually happened. Include the raw statusline output if relevant
(paste the terminal text — colors won't survive, that's fine).

**Payload**
If this is a rendering bug, the JSON payload that triggered it (the same
shape from the harness's stdin — see the README's Testing section for how
to capture one). Redact anything sensitive (paths, session ids).

**Environment**
- OS:
- Python version (`python3 --version`):
- omniline version/commit:
- Harness version:

**Additional context**
Anything else that might help.
